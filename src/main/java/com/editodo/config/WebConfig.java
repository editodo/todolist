package com.editodo.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class WebConfig {
    
    // WebConfig는 웹 관련 설정만 담당
    // RestTemplate은 AppConfig에서 관리
}
